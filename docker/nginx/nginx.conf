user  nginx;
events {
    worker_connections   1000;
}

http {
    server {
        listen 80;
        location / {
            proxy_pass http://backend:8000;
        }

        location /static/ {
            alias /home/app/web/static/;
        }

    }
}